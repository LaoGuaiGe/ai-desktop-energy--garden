# AI 桌面能量花园 —— 智能 USB-HUB 摆件项目方案

> 一棵"住"在 USB-HUB 里的数字花园——你的设备越活跃，花园越有生机。

---

## 一、项目概述

### 1.1 一句话概念

一个多设备 USB-HUB，屏幕上有一座数字花园——你的设备越活跃、专注任务完成得越多，花园里的植物生长、小猫撒娇、鱼儿游动。它是桌面扩展坞，也是桌面上的活体装饰。

### 1.2 核心痛点与解法

| 痛点 | 本项目的解法 |
|------|-------------|
| USB-HUB 无聊，纯功能无乐趣 | 屏幕上有活的数字生态，设备在线 = 花园有生机 |
| 番茄钟/效率工具太枯燥 | 游戏化：专注 → 植物生长 → 解锁新形态 |
| 桌面摆件是死物 | 生态随时间、天气、设备活跃度动态变化 |
| 桌面线材凌乱 | 内置磁吸自动回卷 Type-C 数据线，一键收纳 |
| AI 硬件同质化严重 | 不是又一个 AI 助手，而是有生命感的桌面伴侣 |

### 1.3 核心特色

- **实用 + 情感结合**：多设备 USB-HUB 扩展坞 + 陪伴型桌面摆件
- **游戏化机制**：用生态成长可视化你的工作活跃度和专注行为
- **当前核心生态**：专注番茄 + 可经营花园 + AI 园丁，养猫/养鱼后续扩展
- **AI 园丁**：AI 角色与你互动、提醒、鼓励
- **稳定触摸 UI**：触摸横向轮播、像素 HUD、粒子与收获动效；显示链路以稳定无闪为第一优先级

---

## 二、USB-HUB 功能

### 2.1 数据扩展接口

- **USB-A 3.0 × 2**：5Gbps，支持键鼠 / U 盘 / 外设
- **USB-C 3.0 × 1**：5Gbps，下行数据口

> 精简接口数量以降低成本，统一 USB 3.0（Gen1）规格。

### 2.2 供电与连接

产品采用**独立供电 + 单线连接电脑**的设计，用户日常只需插拔一根线：

```
墙壁插座
    │
    ▼
[12V/2A 适配器] ──DC 口──► [产品背面电源口]（固定不动）

电脑
    │
    ▼
[USB-C 上行口] ──USB-C──► [产品背面上行口]（日常插拔这一根）
```

- **DC 电源口**：12V/2A（24W），背面固定接墙，为产品本身和 HUB 供电
- **DC-DC 降压**：12V → 5V（DC-DC 模块），为下行口提供 5V 电力（自供电 HUB 架构）
- **USB-C 上行口 × 1**：连接电脑，仅传输 HUB 数据（USB 3.0，5Gbps），不从电脑取电
- 产品无需给电脑供电，去掉 PD 直通，降低成本和电路复杂度

### 2.3 磁吸自动回卷数据线

- 内置 **1 条磁吸自动回卷 Type-C 数据线**（USB 3.0，纯数据，无充电功能）
- 弹簧储能回卷，单键一键收线
- 磁吸头无方向盲插，自动吸附对准
- 线长可伸展至 80cm，收纳后仅露出磁吸头
- 线材：TPE 弹性材质，耐弯折 > 10000 次

> 仅保留 1 条 Type-C 回卷线，降低机械复杂度和成本，同时 Type-C 覆盖当前主流设备。

---

## 三、UI 界面设计

### 屏幕规格

| 参数 | 规格 |
|------|------|
| 尺寸 | 6.2 英寸长条屏 |
| 分辨率 | 1280 × 452（横屏 UI，物理屏 452 × 1280 通过 LVGL `sw_rotate` 旋转） |
| 接口 | MIPI-DSI，AXS15260D 面板 |
| 色彩 | 当前 ESP32-P4 工程使用 RGB888，保持画面稳定优先 |
| 刷新率 | 硬件链路以 48MHz DCLK 稳定显示为基线；后续优化以“无白屏、无竖纹、无闪屏”为前提 |
| 触控 | 支持触摸，当前 UI 以横向滑动轮播 + 页面内点击/拖拽为主 |
| 亮度 | 当前支持背光控制，后续接入环境光自动调节 |

> 当前工程已经演进为触摸屏交互。页面之间通过横向滑动切换，页面内部使用点击、拖拽、滑条等直接操作。

### 导航与交互方式

当前 LVGL 架构采用 5 页水平轮播：

| 页面序号 | 页面 | 作用 |
|----------|------|------|
| 0 | Focus | 像素风专注计时与番茄收获 |
| 1 | Garden | 主花园、仓库、种植、浇水、施肥、收获 |
| 2 | AI Chat | AI 园丁表情、语音文本模拟、对话气泡、花园 MCP 控制入口 |
| 3 | Device Center | 设置中心与项目介绍 |
| 4 | Album | 图鉴/相册预留页 |

交互规则：
- 页面切换：在页面空白区域横向滑动，采用 `garden_nav` 统一处理拖拽、吸附和相邻页显示。
- 页面内拖拽：仓库植物拖拽、时间滑条等控件优先消费触摸，避免误触发页面切换。
- 花园操作：点击当前选中土地可浇水或采集；拖拽植物到空土地可种植；拖拽植物到肥料池可转为肥料。
- 专注操作：未开始/暂停时允许滑动底部像素步进器设置 1-60 分钟，运行中锁定设置。
- AI 操作：仿真器中先用模拟语音文本输入，硬件版本接入麦克风 ASR。

### 页面 0：专注模式 Focus

专注页定位为“像素游戏 HUD + 番茄收获仪式”：

```text
┌────────────────────────────────────────────────────────────────────────┐
│  🍅 00:25  │  FOCUS READY / RUNNING / PAUSED        │  ENERGY ...     │
├────────────────────────────────────────────────────────────────────────┤
│                                                                        │
│       [超大 8-bit bitmap 时:分:秒，500ms 冒号闪烁]                      │
│                                                                        │
│       [小草背景 + 根据进度从左到右种下的进度花]                         │
│                                                                        │
├────────────────────────────────────────────────────────────────────────┤
│  [1-60 min 像素步进滑条]     提示栏：Start / Pause / Harvest Tomato     │
└────────────────────────────────────────────────────────────────────────┘
```

已确定的设计点：
- 时间显示使用自定义 bitmap 数字表，不依赖 LVGL 自带大字体。
- 显示时分秒，数字为 8-bit 方块字，带暗边框和外发光；关闭像素格保留透明暗格，形成游戏 HUD 质感。
- 冒号 500ms 闪烁。
- 底部原进度条改为 1-60 分钟像素步进滑条，步进单位 1 分钟。
- 滑条设置的时间实时预览到大时分秒；只有未开始/暂停时可调。
- 中间花园不显示旧的大花，使用小草背景；专注进度以“从左到右种下花”的形式表现，进度花至少比草高。
- 专注完成后出现可收获的大番茄：先在中间逐渐放大，周围有闪烁光圈；用户触摸后，大番茄消失，并飞出对应分钟数的番茄到左上角计数器。
- 番茄计数以图标 + 数量显示；1 分钟 = 1 个番茄，成功完成后才入账。
- ENERGY 保留，暂不作为当前交互重点。

### 页面 1：花园主界面 Garden

花园页定位为“可触摸经营的像素花园”：

```text
┌──────────────┬────────────────────────────────────────────┬──────────────┐
│ 外部天气/温度 │                                            │ 收获仓库      │
│ 桌面温湿度   │     [草地背景 + 5 块种植地 + 1 个肥料池]      │ 3列植物图标   │
│ 设备/环境状态 │     植物 6 阶段成长，土地贴合背景不浮空       │ 番茄/向日葵...│
└──────────────┴────────────────────────────────────────────┴──────────────┘
```

左侧边栏：
- 外部天气与温度：来自天气 API，显示天气图标、室外温度、天气状态。
- 设备周围温湿度：后续接入 SHT30 等传感器；当前可用模拟值占位。
- 可扩展显示：光照强度、空气舒适度、HUB 在线设备数量、今日浇水状态。

中心花园：
- 保留原有草地/天空背景，不使用大面积填充矩形遮挡背景。
- 土地共 6 块，其中 5 块为种植地，最后 1 块为肥料池。
- 土地表现贴近背景，用阴影、土壤纹理、草边和位置关系表达“可种植区域”，避免悬浮框感。
- 每种植物保留 6 个成长阶段：种子、发芽、幼苗、成株、成熟、可收获。
- 当前选中土地在能量满或可操作时显示反馈光圈；旧版单击花园的大花光圈不再保留。
- 浇水与采集需要避免冲突：可收获植物优先采集；不可收获且土地已选中时触发浇水；拖拽过程不触发浇水。

右侧边栏：
- 收获仓库：一排 3 个图标，显示植物完全体图标 + 数量。
- 植物类型先做 6 个：番茄、向日葵、豌豆、蘑菇、辣椒、玉米。
- 仓库中的植物既可以拖到空土地种植，也可以拖到肥料池转为肥料。
- 每次采集可获得 2 个对应植物数量。
- 番茄来源包括专注页番茄收获，保留接口支持后续更多来源。
- 肥料使用独立肥料图标，不复用植物图标；施肥用于加速当前土地植物成长。

植物图标原则：
- 仓库图标必须是对应植物的成熟体，不是抽象种子图标。
- 番茄图标与专注页收获番茄保持一致。
- 向日葵成熟体花圈外有黑边表示成熟花籽。
- 豌豆要突出豆荚/豆粒形态。
- 蘑菇更矮，无叶子，不做高杆植物。
- 辣椒要突出弯曲红椒外形。
- 玉米要突出玉米棒和苞叶结构。

### 页面 2：AI Chat

AI Chat 页定位为“有表情的 AI 园丁控制台”：

```text
┌────────────────────────────────────────────────────────────────────────┐
│ [Robot Face 表情区]     [AI 园丁回复气泡 / 用户语音文本气泡]              │
│ 眼睛/嘴巴/表情状态       用户语音转文字：独立气泡显示                     │
│ 状态：listening/thinking AI 回复：英文文本优先，后续再做中文字体资源       │
│                                                                        │
│ 快捷动作：Harvest / Water / Fertilize / Inspect Garden                 │
└────────────────────────────────────────────────────────────────────────┘
```

设计来源：
- 表情参考 `OLED_UI` 项目中的 robot face 思路，用眼睛、嘴巴和状态动画表达情绪。
- 情绪根据对话状态切换：idle、listening、thinking、happy、concerned、celebrate。
- 仿真器阶段先用模拟语音文本；硬件阶段由麦克风采集语音并转为文字。
- 用户语音文本单独气泡显示，AI 回复单独气泡显示。
- 通过 MCP/函数接口，AI 园丁后续可以执行花园动作：采集植物、浇水、施肥、查询仓库和土地状态。
- 当前屏幕文本优先使用英文，降低字体资源与渲染压力；中文对话作为后续字体方案完善项。

### 页面 3：Device Center

Device Center 定位为“设置 + 项目介绍 + 设备状态中心”：

可设置内容：
- Display：亮度、背光、夜间模式、刷新/性能模式。
- Sound & Haptics：提示音、番茄完成提示、震动强度。
- Network：WiFi/天气/API 连接状态，C6 心跳状态。
- Garden：重置花园、清空仓库、调试成长速度。
- Focus：默认专注分钟数、完成提醒方式。
- About：项目名称、开源说明、硬件信息、固件版本、作者信息。

视觉风格：
- 不做营销落地页，直接做可操作的设置中心。
- 信息卡片保持紧凑，适合 1280×452 横屏快速扫描。
- 重要状态使用图标化表达，减少长文本占用。

### 页面 4：Album / 图鉴

当前作为图鉴与相册预留页：
- 展示已解锁植物、成长阶段、收获记录。
- 后续可加入专注番茄收获历史、花园截图、成就徽章。
- 与分享功能联动，支持保存当前花园状态。

### 动效设计

- 页面切换：横向轮播吸附，拖动时只显示当前页和相邻页，降低渲染压力。
- 专注收获：大番茄放大、光圈闪烁、触摸后多个番茄飞向计数器，节奏参考《植物大战僵尸》的收集反馈。
- 花园粒子：水滴、肥料、成长光点、采集飞行动效；拖拽时避免触发页面切换。
- AI 表情：眼睛眨动、看向说话气泡、思考动画、完成动作后的庆祝表情。
- 天气与环境：后续接入天气 API 后显示雨、云、光照等轻量动效。

## 四、虚拟生态系统

> 当前 MVP 聚焦“种植 + 专注番茄 + AI 园丁”这一条闭环。养猫、养鱼等生态不删除概念，但后移到 V2/V3，避免 MVP 分散。

### 4.1 当前 MVP：能量花园

核心循环：

```text
专注计时完成 → 收获番茄 → 番茄进入仓库 → 拖拽种植/转肥料 → 植物成长 → 采集更多植物 → 扩展花园库存
```

主要资源：
- 番茄：由专注页产生，1 分钟 = 1 个番茄，完成后通过收获动画进入仓库。
- 植物库存：花园采集获得，每次采集 +2 个对应植物。
- 肥料：将仓库植物拖入肥料池转换得到，用于加速当前选中土地的植物成长。
- ENERGY：保留为全局能量接口，暂不作为当前版本的强交互重点。

### 4.2 植物系统

首批植物 6 种：

| 植物 | 来源 | 用途 | 图标要求 |
|------|------|------|----------|
| 番茄 | 专注收获 / 花园采集 | 种植、转肥料、专注成果展示 | 与专注页收获番茄完全一致 |
| 向日葵 | 花园采集 / 后续奖励 | 种植、转肥料 | 花盘外圈黑边，表现成熟花籽 |
| 豌豆 | 花园采集 / 后续奖励 | 种植、转肥料 | 明确豆荚和豆粒形态 |
| 蘑菇 | 花园采集 / 后续奖励 | 种植、转肥料 | 矮小，无叶子，不做高杆 |
| 辣椒 | 花园采集 / 后续奖励 | 种植、转肥料 | 弯曲红椒，顶部有蒂 |
| 玉米 | 花园采集 / 后续奖励 | 种植、转肥料 | 玉米棒 + 苞叶结构 |

每个植物保留 6 个成长阶段：

| 阶段 | 含义 | 视觉要求 |
|------|------|----------|
| 0 | 空地/待种植 | 贴近背景的土地，不用厚重矩形框 |
| 1 | 种下 | 小种子或土壤破口 |
| 2 | 发芽 | 低矮嫩芽 |
| 3 | 幼苗 | 植物轮廓初现 |
| 4 | 成株 | 接近成熟体但不可采集 |
| 5 | 成熟可收获 | 完全体 + 轻量反馈光圈 |

### 4.3 土地与肥料池

- 花园中心区域共有 6 块土地。
- 前 5 块为普通种植地，可种植仓库植物。
- 第 6 块为肥料池，拖拽植物进去会转为肥料。
- 肥料使用独立图标和数量，不复用番茄或其它植物图标。
- 施肥动作绑定当前选中土地，避免“拖到哪里都生效”的歧义。

### 4.4 后续生态扩展

养猫、养鱼仍保留为后续方向，但不进入当前 MVP 主线：
- 养猫模式：作为桌面陪伴生态，可使用花园收获物投喂。
- 养鱼模式：作为长条屏横向水景，可和天气、时间联动。
- 其它生态：未来可通过开源接口扩展养兔、种菜、桌面天气站等玩法。

## 五、AI 交互功能

### 5.1 AI 园丁定位

AI 园丁不是单纯聊天窗口，而是花园里的“有表情的操作代理”：
- 能根据对话状态显示不同表情。
- 能把用户语音转文字显示在独立气泡里。
- 能用英文回复气泡进行轻量交流，后续再扩展中文字体资源。
- 能通过 MCP/函数接口控制花园动作：采集、浇水、施肥、查看库存、查看土地状态。

### 5.2 表情系统

表情实现参考 `OLED_UI` 的 robot face 方案，用简单几何图形表达眼睛、嘴巴和状态动画：

| 状态 | 触发场景 | 表情表现 |
|------|----------|----------|
| Idle | 默认待机 | 平静眨眼，视线轻微移动 |
| Listening | 语音采集中 | 眼睛张大，嘴巴小幅波动 |
| Thinking | 等待 AI 回复 | 眼睛左右扫描，显示思考节奏 |
| Happy | 操作成功/鼓励用户 | 弯眼笑脸，轻微弹跳 |
| Concerned | 花园缺水/任务失败 | 眉眼下压，嘴角收起 |
| Celebrate | 专注完成/收获成功 | 亮眼、张嘴笑、短暂闪光 |

### 5.3 语音与对话

分阶段实现：

| 阶段 | 方案 | 说明 |
|------|------|------|
| 仿真器阶段 | 模拟语音输入 | 用固定按钮或模拟文本生成“用户语音文本”气泡 |
| ESP32-P4 原型 | 麦克风采集 + 云端 ASR | INMP441 采集音频，C6 联网转文字 |
| 后续版本 | ASR + AI + TTS | 用户语音 → 文本 → AI 回复 → 语音播报 |

显示规则：
- 用户语音文本单独气泡显示，避免和 AI 回复混在一起。
- AI 回复优先短句，适配 1280×452 长条屏。
- 当前 UI 文本优先英文，减少字体资源压力；中文 UI 后续通过字体子集化处理。

### 5.4 AI 控制花园动作

AI 园丁可调用的动作接口规划：

| 动作 | 参数 | 效果 |
|------|------|------|
| `inspect_garden` | 无 | 返回土地、仓库、肥料、可采集状态 |
| `water_plot` | plot_id | 给指定土地浇水 |
| `fertilize_plot` | plot_id, amount | 对指定土地施肥，加速成长 |
| `harvest_plot` | plot_id | 采集成熟植物，仓库数量 +2 |
| `plant_crop` | plant_type, plot_id | 从仓库扣除 1 个植物并种到空土地 |
| `convert_to_fertilizer` | plant_type, count | 将仓库植物转换为肥料 |

安全规则：
- AI 动作必须经过当前 UI 状态校验，不能凭空增加库存。
- 涉及消耗植物或肥料的动作，后续硬件版可增加用户确认。
- 仿真器优先验证交互体验，真实联网后再接入大模型 API。

### 5.5 云端 AI 与降级

- 可接入 GLM、DeepSeek、OpenAI 等云端模型，首版建议非流式问答，降低 ESP-AT 分片处理风险。
- API 不可用时降级为本地模板对话。
- 本地模板需要能覆盖：专注鼓励、收获提醒、缺水提醒、仓库满提醒、设置引导。

## 六、扩展功能

### 6.1 传感器扩展

| 传感器 | 用途 |
|--------|------|
| 光敏电阻 / BH1750 | 环境亮度，自动调节屏幕亮度 |
| SHT30 温湿度 | 显示桌面温湿度，花卉生长与温度联动 |
| VCNL4040 接近传感器 | 检测用户靠近，自动唤醒屏幕 |
| MPU6050 陀螺仪 | 检测摆件被移动，触发宠物"晕车"动画 |
| INMP441 麦克风 | 语音唤醒，语音投喂指令 |

### 6.2 氛围灯系统

- 底部 RGB LED 环形灯带（WS2812B × 16 颗）
- 连接状态呼吸灯：有设备连接时缓慢呼吸，无设备时常亮
- 生态联动：猫咪睡觉时暖黄色，鱼缸模式时蓝色波纹
- 音乐律动模式：麦克风拾音，灯光跟随节拍跳动
- 夜间自动降低亮度

### 6.3 声音系统

- 内置小型扬声器（1W，MAX98357A 功放）
- 投喂音效：猫咪"喵"、鱼儿"咕噜"、浇水声
- 连接提示音：USB-C 上行口连接电脑时轻柔提示音
- 白噪音模式：雨声 / 溪流声 / 森林声（助眠）
- AI 语音播报（V1 预录音频 / V2 云端 TTS 合成）

### 6.4 社交 & 成就系统

- **成就徽章**：首次连接全部 3 个下行口、连续养护 30 天、解锁全部宠物
- **打卡系统**：每日签到获得"阳光值"，用于购买装饰道具
- **分享功能**：通过 App 截图花园状态分享至社交平台
- **好友互访**：局域网内两台设备可互相"串门"，宠物互动

### 6.5 个性化定制

- 宠物命名：通过 App 或屏幕键盘输入
- 主题皮肤：赛博朋克 / 日式和风 / 北欧简约 / 像素风
- 节日限定：春节 / 圣诞 / 万圣节特别场景自动激活
- 壁纸自定义：上传照片作为花园背景

### 6.6 App 联动（蓝牙 / WiFi）

- 远程查看宠物状态
- 推送通知：宠物饥饿提醒、设备连接提醒
- 固件 OTA 升级
- 社区生态商店（免费下载社区贡献的皮肤/宠物/植物）

### 6.7 WiFi 首次配网

| 方案 | 说明 | 优先级 |
|------|------|--------|
| **BLE 配网（首选）** | ESP32-C6 BLE 5.0 广播，手机 App 通过蓝牙发送 WiFi SSID/密码 | MVP |
| 屏幕键盘手动输入 | 触摸屏可支持简化屏幕键盘，但长条屏输入效率有限，仍建议 App 配网 | V2 备选 |
| SmartConfig | 手机 App 广播 WiFi 信息，设备嗅探获取 | V2 可选 |

> MVP 阶段首选 BLE 配网（C6 原生支持 BLE 5.0，配对体验好）；若用户无手机 App，可通过屏幕键盘手动输入。

---

## 七、硬件方案

### 7.1 主控方案

**主控：ESP32-P4 + ESP32-C6（WiFi 协处理器）**

| 芯片 | 职责 | 规格 |
|------|------|------|
| ESP32-P4 | UI 渲染 / 业务逻辑 / USB HUB 管理 / 传感器 | 双核 RISC-V，768KB SRAM，支持 32MB PSRAM，MIPI-DSI，PPA 能力预留，USB 2.0 HS |
| ESP32-C6 | WiFi / BLE / AI API / 天气 / OTA | 单核 RISC-V @ 160MHz，WiFi 6，BLE 5.0 |

两芯片通过 **UART** 通信，C6 运行 ESP-AT 固件，P4 用 AT 指令控制网络请求。

### 7.2 硬件清单（BOM）

| 部件 | 型号 | 参考价（小批量） |
|------|------|--------|
| 主控 | ESP32-P4 模组（含 32MB PSRAM） | ¥65 |
| WiFi 协处理器 | ESP32-C6 模组 | ¥8 |
| 屏幕 | 6.2 英寸 1280×452 MIPI-DSI 触摸长条屏 | ¥60-100 |
| USB HUB 芯片 | GL3523（USB 3.0，3 口使用）| ¥12 |
| DC 电源模块 | DC 5.5mm 母座 + 12V/2A 适配器 | ¥8 |
| DC-DC 降压模块 | 12V → 5V（供 HUB 下行口 5V） | ¥5 |
| 磁吸自动回卷线 × 1 | Type-C，弹簧回卷 + 磁吸头 | ¥20 |
| 麦克风 | INMP441 I2S | ¥5 |
| 扬声器 + 功放 | 8Ω 1W + MAX98357A | ¥10 |
| RGB LED | WS2812B-2020 × 16 | ¥5 |
| 传感器组 | BH1750 + VCNL4040 | ¥8 |
| RTC | DS3231 | ¥5 |
| 触摸输入 | 屏幕触摸面板，作为主要交互输入 | 随屏幕 |
| 确认键 | 金属轻触开关 | ¥2 |
| 震动电机 | 扁平线性马达（LRA） | ¥5 |
| 外壳 | 3D 打印"黑匣"造型（SLA） | ¥40 |
| 配重块 | 钢板冲压 | ¥3 |
| PCB + 结构件 | 4 层板 | ¥35 |
| 其他 | 线材 / 连接器 / 磁铁 | ¥12 |
| **合计** | | **≈ ¥313** |

> MVP 原型阶段可精简至 ¥150（ESP32-P4 EV Board + 长条屏 + GL3523 HUB + ESP32-C6，使用开发板而非自制 PCB）
>
> 注：相比 v7.0 方案，BOM 大幅降低原因：屏幕从常规 6.2" IPS 方案调整为 1280×452 MIPI 长条触摸屏；去掉 VL822/IT6263（无 HDMI/USB-C 扩展）；3 条磁吸线改为 1 条回卷线；USB 口从 4+2 减为 2+1。量产（1000+）时 BOM 可降至 ¥250 左右。

### 7.3 技术栈

| 层级 | 技术选型 |
|------|----------|
| 主控平台 | ESP32-P4（ESP-IDF v5.3+）|
| WiFi 协处理器 | ESP32-C6（ESP-AT 固件）|
| 图形框架 | LVGL 9.5（当前 PPA 默认关闭，稳定后再逐项验证硬件加速）|
| UI 设计工具 | Squareline Studio（可视化，自动生成代码）|
| AI 服务 | GLM-4-Flash / DeepSeek / Claude API |
| 语音 | INMP441 录音 + TTS 合成播报 |
| 数据存储 | NVS Flash（宠物状态持久化）|
| 联网 | ESP32-C6 WiFi 6（天气 / 时间 / AI API）+ BLE（App 配对）|

---

## 八、软件架构

```text
┌──────────────────────────────────────────────────────┐
│                   ESP32-P4（主控）                     │
│                                                      │
│              应用层 (App Layer)                       │
│  Focus │ Garden │ AI Chat │ Device Center │ Album     │
├──────────────────────────────────────────────────────┤
│              UI 层 (LVGL 9.5)                         │
│  garden_nav 水平轮播 │ 页面对象 │ 触摸事件 │ 动效定时器   │
├──────────────────────────────────────────────────────┤
│           业务逻辑层 (Game Logic)                      │
│  专注计时 │ 番茄收获 │ 仓库 │ 土地 │ 肥料 │ AI 动作接口 │
├──────────────────────────────────────────────────────┤
│           渲染层                                      │
│  Canvas 场景绘制 │ bitmap 像素数字 │ 植物图标 │ 粒子/飞行动效 │
├──────────────────────────────────────────────────────┤
│           驱动层 (HAL)                                │
│  MIPI-DSI │ 触摸 │ 背光 │ HUB IC │ 传感器 │ 音频 │ RTC       │
├──────────────────────────────────────────────────────┤
│           通信层 (UART → ESP32-C6)                    │
│  WiFi 天气 │ AI API │ BLE/OTA │ 时间同步                 │
└──────────────────────────────────────────────────────┘
```

### 8.1 当前代码结构

| 文件/模块 | 职责 |
|-----------|------|
| `garden_nav.c/h` | 5 页面水平轮播、页面注册、拖拽、吸附、相邻页显示 |
| `garden_focus.c/h` | 专注页 UI、像素时间、时间滑条、番茄收获动画 |
| `garden_page.c/h` | 花园页 UI、土地、植物成长、仓库、肥料、浇水和采集 |
| `garden_ai.c/h` | AI Chat 页面、表情园丁、语音文本模拟、快捷动作入口 |
| `garden_device.c/h` | Device Center 设置与项目介绍 |
| `garden_album.c/h` | 图鉴/相册预留页 |
| `main.c` | LCD/MIPI/LVGL 初始化、页面创建与注册、主循环 tick |

### 8.2 关键设计原则

- **稳定优先**：AXS15260D 面板当前以 48MHz DCLK、RGB888、PPA 关闭为稳定基线。任何帧率优化必须逐项验证，不能牺牲显示稳定性。
- **触摸优先**：页面根对象和可交互控件必须正确处理 LVGL 事件，内部拖拽不能误触发导航滑动。
- **页面解耦**：导航只负责页面位置和切换；专注、花园、AI、设置各自维护内部状态。
- **资源节制**：只使用 `lv_font_montserrat_14`，大数字用自定义 bitmap，植物和表情尽量用 Canvas/矢量像素绘制减少字体与图片资源压力。
- **仿真先行**：UI 和交互优先在 `garden-ui-sim` 中快速验证，再移植到 ESP32-P4 工程。
- **硬件差异隔离**：仿真器保留相同页面结构和逻辑接口，硬件相关的 MIPI、触摸、背光、传感器通过适配层替换。

### 8.3 性能与显示策略

当前硬件验证结论：
- DCLK 61MHz、56MHz、52MHz 均出现白屏、竖纹或显示异常，48MHz 为稳定基线。
- PPA 开启后在当前 `sw_rotate` + 面板链路下出现白屏，暂不作为默认方案。
- CPU 400MHz 试验触发早期启动断言，当前保持 360MHz。
- LVGL SYSMON / PERF_MONITOR 不开启；如需观测性能，使用轻量日志或开发者页面。

后续优化方向：
- 减少大面积 Canvas 每帧重绘，拆分静态背景和动态对象。
- 拖拽页面时降低非当前页动画频率。
- 优化花园粒子数量和绘制区域，优先保证触摸跟手感。
- 任何显示链路优化按“一次只改一个变量”的方式测试并记录。

### 8.4 开发者模式

Device Center 可预留隐藏开发者模式，用于调试：
- 页面 FPS / tick 频率。
- PSRAM / SRAM 剩余量。
- 当前页面、拖拽状态、触摸坐标。
- 土地、仓库、肥料、番茄数量。
- C6/WiFi/API/天气连接状态。

开发者模式默认关闭，不使用 LVGL 内置 sysmon/perf monitor。

## 九、外观设计

### 设计理念

**"紧凑利刃"** —— 整机紧紧包裹屏幕，不浪费一毫米。楔形剖面前薄后厚，像一把精密折刀横卧桌面。45° 倒角露出银色底层形成双色对比，赋予精密机械感。设备存在感极低——桌面上只看到一条发光的花园画卷。

### 整体造型

```
                    正面视图（用户视角）
    ┌───────────────────────────────────────────────────┐
    │ ┌───────────────────────────────────────────────┐ │
    │ │        1280×452 长条屏（155×56.5mm）            │ │  ← 3mm 极窄边框
    │ │      花园全景 / 番茄钟 / AI 对话                │ │
    │ └───────────────────────────────────────────────┘ │
    └───────────────────────────────────────────────────┘
     ▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔  ← 1.5mm LED 光缝

                    侧面视图（左侧，楔形剖面）
                ┌──────────────────────┐
               ╱  顶面窄条（编码器+按钮） ╲  ← 后高 22mm
              ╱                          │
             ╱    屏幕（微仰 8°）          │
            ╱______________________________│  ← 前低 12mm
            ▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔▔  ← LED 光缝

                    俯视图
    ┌───────────────────────────────────────────────────┐
    │  ○ 磁吸线缝隙                          ◉  ●      │ ← 编码器+按钮在顶面后部
    │                                                   │
    │              屏幕区域（正面可见）                    │
    └───────────────────────────────────────────────────┘
                                            ↑ 顶面窄条仅 12mm 深

                    背面视图
    ┌───────────────────────────────────────────────────┐
    │    [USB-A] [USB-A] [USB-C↓]   [USB-C↑上行] [DC]  │
    └───────────────────────────────────────────────────┘
      接口内凹 3mm
```

**关键设计要素**：

| 要素 | 设计 |
|------|------|
| 整机尺寸 | 165mm × 68mm × 22mm（长×深×高），紧凑致密 |
| 楔形剖面 | 前缘 12mm / 后缘 22mm，屏幕自然微仰 8° |
| 屏幕占比 | 正面 90% 为屏幕，仅 3mm 边框 |
| 触摸屏 | 正面作为主要交互区域，支持滑动、点击、拖拽和滑条调节 |`r`n| 备用实体键 | 顶面或背面预留 1 个隐藏功能键，用于复位、开发者模式或量产测试 |
| 倒角双色 | 全周 45° 倒角（2mm 宽），露出银色铝合金底层，形成精密边框线 |
| 磁吸回卷线 | 左侧面发丝级缝隙伸出，收回后完全不可见 |
| USB 接口 | 背面内凹槽（深 3mm） |
| RGB 灯带 | 底部 1.5mm 缝隙，光线向下打在桌面形成一条细光线 |
| 扬声器 | 底面出音，通过底部缝隙扩散 |
| 麦克风 | 顶面 0.8mm 针孔 |
| 震动电机 | 内置于编码器下方 |

### 造型细节

- **极致紧凑**：设备仅比屏幕大一圈（每边多 5-6mm），拿在手里像一块加厚的手机
- **楔形张力**：前薄后厚的剖面让设备有"俯冲"的动势，不呆板
- **倒角光边**：45° CNC 倒角贯穿四周，银色亮边在深色机身上形成精确的几何轮廓线
- **顶面极简**：仅有编码器和按钮两个元素，其余完全平整
- **分割线**：顶壳与底座之间 0.3mm 分模线，同时是 LED 出光缝隙
- **重量**：内置钢板配重，整机约 250g，单手可握但不会被线材拽动
- **表面**：全机身喷砂哑光，无 Logo（激光蚀刻同色 Logo，特定角度可见）

### 材质 & 工艺

| 部位 | 材质 | 工艺 |
|------|------|------|
| 外壳主体 | 铝合金 6063 / PC+ABS（量产） | CNC（原型）/ 注塑+喷砂（量产） |
| 屏幕面板 | 钢化玻璃 0.55mm | 全贴合，AF 防指纹镀膜 |
| 编码器旋钮 | 铝合金 | CNC + 黑色阳极氧化 + 直纹滚花 |
| 确认键 | 不锈钢 304 | 拉丝处理，方形 |
| 底座 | 铝合金 / PC（量产） | 喷砂哑光，底部硅胶防滑条 |
| 配重 | 钢板 | 冲压，内置 |

### 配色方案

| 方案 | 机身 | 倒角/亮边 | 灯带默认色 | 风格 |
|------|------|----------|-----------|------|
| **深空黑**（主推） | 纯黑哑光 | 银色铝合金 | 冷白 | 隐形科技 |
| **枪灰** | 深灰哑光 | 亮银 | 青色 | 精密机械 |
| **月岩白** | 浅灰白哑光 | 深灰 | 暖白 | 极简桌面 |

---

## 十、开发路线图

> 注：时间估算基于单人/小团队全职开发，含调试和迭代时间。

### Phase 1 — 硬件原型验证（1-2 周，预留 1 周 buffer）
- [ ] ESP32-P4 官方 EV Board + 1280×452 长条屏 MIPI-DSI 点亮（含 Init Sequence 调试）
- [ ] LVGL 9.5 跑通，先保持 PPA 关闭验证稳定显示，再逐项测试硬件加速
- [ ] 加载中文子集字体，测试 1280×452 下文字渲染帧率
- [ ] 触摸输入、页面滑动导航、按钮/拖拽/滑条交互 + 基础 UI 框架（横屏布局）
- [ ] ESP32-C6 AT 固件烧录，UART DMA 通信验证
- [ ] **里程碑**：屏幕稳定显示 LVGL Demo + 编码器/按钮响应正常 + 震动反馈正常

**Phase 1 结束 — Go/No-Go 决策点：**

```
Go 条件（全部满足）：
  ✅ MIPI-DSI 长条屏稳定显示 LVGL Demo 超过 1 小时无花屏
  ✅ 稳定显示无白屏/竖纹/闪屏，触摸滑动响应稳定
  ✅ 触摸点击、拖拽、页面滑动响应延迟可接受
  ✅ 屏幕供应商确认可小批量供货（< 4 周交期）

No-Go（任一触发 → 切换备选方案）：
  ❌ 屏幕无法稳定点亮超过 10 分钟
  ❌ 驱动 IC 资料缺失且供应商不配合
  ❌ 当前稳定配置下页面滑动严重撕裂或触摸不可用
  → 备选方案：ESP32-S3 + RGB/SPI 屏（降低分辨率，保证稳定性）
```

### Phase 2 — UI 开发（2-4 周）
- [ ] Squareline Studio 设计 4 个页面（适配 1280×452 横屏布局）
- [ ] 植物动画（PNG 序列帧，存 SPI Flash）— MVP 仅做种花模式
- [ ] 页面切换动画 + 粒子效果（先软件稳定，后续再评估硬件加速）
- [ ] 天气 API 对接（通过 C6 AT 指令）+ 天气动画
- [ ] **里程碑**：完整 UI 流程可通过编码器+按钮操作，动效流畅

### Phase 3 — 核心逻辑（1-2 周）
- [ ] 种花模式属性系统（MVP 仅做花，猫/鱼推迟到 V2）
- [ ] 能量系统 + 植物进化状态机
- [ ] 番茄钟功能
- [ ] 数据持久化（NVS Flash）
- [ ] **里程碑**：完整的种花 + 番茄钟游戏循环可运行

### Phase 4 — AI 集成（2-3 周）
- [ ] 通过 C6 接入大模型 API（GLM-4-Flash / DeepSeek，非流式）
- [ ] AT 指令压力测试（连续 1000 次请求，验证 ESP-AT 稳定性和分片重组）
- [ ] AI 园丁角色设定 + 预设对话库
- [ ] 每日一句 + 主动提醒（基于状态机触发）
- [ ] 一键截图分享功能（LVGL 截屏 → BLE 发送到手机保存）
- [ ] 语音输入（INMP441 录音 → 云端 ASR）— 可选，视进度决定
- [ ] **里程碑**：AI 园丁可对话，响应时间 < 5 秒；截图可通过 BLE 传输

### Phase 5 — 硬件集成（3-5 周）
- [ ] GL3523 HUB 集成（使用 3 口：USB-A × 2 + USB-C × 1），端口连接状态检测
- [ ] DC 电源口 + DC-DC 降压（12V→5V 供 HUB 下行口）+ USB-C 上行口集成
- [ ] 磁吸自动回卷 Type-C 线机构集成
- [ ] PCB 设计 + 打样 + 焊接 + 调试（预留至少 2 轮迭代）
- [ ] RGB LED 氛围灯 + 生态联动
- [ ] 传感器集成（光感 / 接近）
- [ ] **里程碑**：自制 PCB 板上所有功能正常工作

### Phase 6 — 外壳 & 打磨（2-4 周）
- [ ] 3D 打印花园造型外壳（适配 155mm×56.5mm 长条屏，预留 2-3 轮迭代）
- [ ] 整体组装 + 调试
- [ ] 亮度自适应、省电模式、细节打磨
- [ ] **里程碑**：完整可展示的工程样机

### 总体里程碑

| 阶段 | 目标 | 预计时间 |
|------|------|----------|
| MVP 原型（V1） | 种花模式 + HUB 基础功能 + AI 对话 + 截图分享 | 约 3 个月 |
| V2a OTA 更新 | 养猫 Beta + 社交分享模板 | 发货后 2 个月 |
| V2b 工程样机 | 养鱼 + TTS + App + 完整外壳 + 磁吸回卷线 | 约 6 个月 |
| 量产准备 | 认证 + 开模 + 供应链锁定 | 约 9-12 个月 |

---

## 十一、MVP 范围定义

当前 MVP 目标是验证“触摸长条屏 + 像素专注 + 可经营花园 + AI 园丁”的核心体验。

### 11.1 MVP 包含（V1）

| 模块 | 范围 |
|------|------|
| 页面导航 | 5 页水平轮播，支持触摸滑动切换 |
| Focus | 自定义 bitmap 像素时分秒、1-60 分钟滑条、500ms 冒号闪烁、番茄收获动画 |
| Garden | 5 块种植地 + 1 个肥料池、6 种植物、6 阶段成长、仓库、浇水、施肥、采集 |
| AI Chat | 表情园丁、用户语音文本模拟、AI 回复气泡、英文文本优先、花园动作接口预留 |
| Device Center | 设置中心、项目介绍、显示/声音/网络/花园/专注配置入口 |
| Album | 图鉴/相册/成就预留页 |
| 硬件显示 | ESP32-P4 + 1280×452 MIPI DSI，稳定显示优先 |
| 仿真器 | `garden-ui-sim` 用于快速调 UI，再同步移植到 ESP32-P4 工程 |
| 资源约束 | 仅使用 `lv_font_montserrat_14`；大数字和图标采用自绘/bitmap 方案 |

### 11.2 MVP 暂不包含

| 模块 | 原因 |
|------|------|
| 养猫 / 养鱼完整生态 | 当前主线聚焦能量花园，避免内容分散 |
| 完整中文字体库 | 资源和性能压力较大，先用英文文本 + 图标化 UI |
| PPA 默认开启 | 当前硬件链路验证存在白屏风险，先保持稳定 |
| 复杂联网 AI 流式输出 | ESP-AT 分片和延迟风险较高，先做非流式/模拟接口 |
| 完整 App 社区系统 | 后续版本扩展 |

### 11.3 V2 方向

- 接入真实天气 API，驱动花园天气状态。
- 接入麦克风 ASR 和云端 AI 回复。
- 增加 TTS 或提示音反馈。
- 完善 Album 图鉴、收获历史、成就系统。
- 加入更多植物、装饰、主题皮肤。

### 11.4 V3 远期

- 养猫、养鱼等新生态。
- 社区贡献生态模块。
- 手机 App、分享模板、远程管理。
- 本地唤醒词或更完整的语音交互。

> 原则：MVP 不追求功能数量，而追求一条完整且好玩的闭环：专注获得番茄，番茄进入花园，花园产生植物，植物又能种植或转肥料。

## 十二、差异化竞争优势

| 对比项 | 现有产品 | 本项目 |
|--------|----------|--------|
| 形态 | 死物 USB-HUB / 桌面机器人 | 活体花园扩展坞 |
| 屏幕用途 | 无 / 显示对话文字 | 动态生态 + 信息 |
| 交互 | 纯语音 / 无 | 实体按钮 + 语音 + 设备感应 |
| 持续动力 | 无 | 游戏化成长系统 |
| 充电功能 | 基础充电 / 无 | 无（独立 DC 供电，无充电管理风险）|
| 情感连接 | 弱 / 无 | 强（养宠物 / 种花 / 养鱼）|

---

## 十三、数据结构设计

### 13.1 全局花园状态（NVS 持久化）

```c
typedef enum {
    PLANT_TOMATO = 0,
    PLANT_SUNFLOWER,
    PLANT_PEA,
    PLANT_MUSHROOM,
    PLANT_PEPPER,
    PLANT_CORN,
    PLANT_COUNT,
} plant_type_t;

typedef struct {
    uint32_t tomato_count;                 // 专注页和花园采集获得的番茄数量
    uint16_t plant_inventory[PLANT_COUNT]; // 仓库中 6 种植物数量
    uint16_t fertilizer_count;             // 肥料数量
    uint16_t energy_x10;                   // ENERGY 预留，定点数
    uint32_t total_focus_sec;              // 累计专注秒数
    uint32_t completed_focus_sessions;     // 完成的专注次数
    uint32_t achievements;                 // 成就位图
} garden_state_t;
```

### 13.2 专注计时状态

```c
typedef enum {
    FOCUS_IDLE = 0,
    FOCUS_RUNNING,
    FOCUS_PAUSED,
    FOCUS_DONE_WAIT_HARVEST,
    FOCUS_HARVEST_ANIM,
} focus_state_t;

typedef struct {
    focus_state_t state;
    uint16_t target_minutes;     // 1-60，可由像素步进滑条设置
    uint32_t remaining_sec;      // 当前剩余秒数
    uint32_t elapsed_sec;        // 当前已专注秒数
    bool colon_visible;          // 500ms 冒号闪烁
    bool harvest_ready;          // 是否显示中间大番茄
    uint16_t pending_tomatoes;   // 本次完成后待飞入仓库的番茄数
} focus_timer_t;
```

### 13.3 土地与植物状态

```c
#define GARDEN_PLOT_COUNT 6
#define GARDEN_PLANT_PLOTS 5
#define GARDEN_FERTILIZER_PLOT 5

typedef enum {
    PLOT_EMPTY = 0,
    PLOT_GROWING,
    PLOT_READY,
    PLOT_FERTILIZER_POOL,
} plot_state_t;

typedef struct {
    plot_state_t state;
    plant_type_t plant;
    uint8_t growth_stage;        // 0-5，保留 6 阶段成长
    uint16_t growth_x100;        // 0-10000，避免浮点
    uint32_t last_water_ts;
    bool selected;
    bool harvest_glow;
} garden_plot_t;
```

### 13.4 AI 园丁状态

```c
typedef enum {
    GARDENER_IDLE = 0,
    GARDENER_LISTENING,
    GARDENER_THINKING,
    GARDENER_HAPPY,
    GARDENER_CONCERNED,
    GARDENER_CELEBRATE,
} gardener_emotion_t;

typedef struct {
    gardener_emotion_t emotion;
    char user_text[96];          // 用户语音转文字气泡
    char assistant_text[160];    // AI 回复气泡
    bool awaiting_response;
    uint32_t last_action_ts;
} ai_gardener_state_t;
```

### 13.5 HUB 状态（实时，不持久化）

```c
typedef struct {
    uint8_t  port_count;        // 总端口数（3 个下行口）
    uint8_t  connected_count;   // 当前已连接设备数
    bool     port_a[2];         // USB-A × 2 各口连接状态
    bool     port_c_down;       // USB-C 下行口连接状态
    bool     host_connected;    // USB-C 上行口是否连接电脑
    uint32_t uptime_sec;        // 本次连接持续时间（秒）
} hub_status_t;
```

### 13.6 事件总线（核心事件类型）

| 事件 ID | 触发条件 | 订阅方 |
|---------|----------|--------|
| `EVT_NAV_PAGE_CHANGED` | 页面滑动吸附完成 | 当前页 tick/动画控制 |
| `EVT_FOCUS_DONE` | 专注倒计时归零 | Focus UI、番茄收获系统 |
| `EVT_TOMATO_HARVESTED` | 用户触摸收获大番茄 | 仓库、飞行动效、AI 园丁 |
| `EVT_PLANT_DROPPED` | 仓库植物拖到土地 | 花园土地系统 |
| `EVT_PLANT_TO_FERTILIZER` | 植物拖到肥料池 | 仓库、肥料系统 |
| `EVT_WATER_PLOT` | 点击已选土地浇水 | 花园成长系统、粒子效果 |
| `EVT_FERTILIZE_PLOT` | 对选中土地施肥 | 花园成长系统、肥料库存 |
| `EVT_HARVEST_PLOT` | 成熟植物采集 | 仓库 +2、采集飞行动效 |
| `EVT_AI_ACTION` | AI 园丁调用动作接口 | 花园/专注/设置模块 |
| `EVT_DEVICE_IN` | 设备插入 HUB 端口 | UI 动效、能量系统 |
| `EVT_DEVICE_OUT` | 设备拔出 | UI 动效 |

## 十四、关键 API 接口

### 14.1 天气 API（和风天气 / OpenWeatherMap）

```
GET https://devapi.qweather.com/v7/weather/now
    ?location={lon},{lat}&key={API_KEY}

响应字段使用：
  now.text    → 天气描述（晴/多云/雨）
  now.temp    → 温度
  now.icon    → 天气图标代码（映射本地图标资源）
```

刷新策略：每 30 分钟请求一次，失败时使用缓存值。

### 14.2 AI 对话 API

```
POST https://open.bigmodel.cn/api/paas/v4/chat/completions

System prompt 模板：
"你是一位温柔的数字花园园丁，名叫[宠物名]。
 当前花园状态：{mode}模式，{stage}阶段，能量{energy}。
 今日天气：{weather}，时间：{time}。
 用不超过30字回复，语气活泼可爱。"
```

### 14.3 HUB 端口状态检测（GL3523，GPIO）

```c
// 通过 GPIO 检测各端口连接状态
// GL3523 提供 VBUS 检测引脚，高电平表示设备已连接
#define HUB_PORT_A1_PIN  GPIO_NUM_4
#define HUB_PORT_A2_PIN  GPIO_NUM_5
#define HUB_PORT_C1_PIN  GPIO_NUM_6

uint8_t read_connected_count() {
    uint8_t count = 0;
    int pins[] = {HUB_PORT_A1_PIN, HUB_PORT_A2_PIN, HUB_PORT_C1_PIN};
    for (int i = 0; i < 3; i++) {
        if (gpio_get_level(pins[i])) count++;
    }
    }
    return count;
}
```

---

## 十五、风险与挑战

### 15.1 技术风险

| 风险 | 概率 | 影响 | 应对方案 |
|------|------|------|----------|
| **MIPI-DSI 屏驱动不通**（面板 Init Sequence 调试、高速差分布线） | 高 | **致命** | MVP 阶段用官方 EV Board + FPC 排线连接，不自行画 PCB；备选 RGB 并行接口屏 + 转接芯片；提前确认屏幕供应商提供完整驱动 IC 资料 |
| ESP32-P4 内存不足（LVGL + 音频 + 业务逻辑同时运行）| 低 | 中 | 外挂 32MB PSRAM（非 16MB）；双缓冲帧缓冲放 PSRAM |
| 磁吸回卷线机构可靠性差（卡线/断线/Pogo Pin 接触不良）| 高 | 高 | **MVP 阶段不做**，用普通磁吸收纳线代替；V2 版本再上机械回卷 |
| HUB 芯片与 ESP32-P4 之间的 USB 枚举冲突 | 中 | 中 | HUB 芯片独立供电；P4 仅做状态监测不介入 USB 协议 |
| AI API 延迟高（>3s）影响体验 | 中 | 中 | 首版用非流式单次问答；本地缓存常用回复；预设对话库兜底 |
| ESP-AT 流式响应处理不稳定（chunked transfer 支持有限）| 中 | 中 | 首版不做流式输出；AT 指令预留 SPI 备用通信方式 |
| 长条屏 LVGL 帧缓冲（1280×452×2B = 1.1MB）| 低 | 低 | 16MB PSRAM 即可满足，双缓冲仅 2.2MB |
| 1080P 全屏粒子效果帧率不达标 | 中 | 中 | 当前优先控制闪屏和撕裂；限制同屏粒子数 ≤ 30，减少大面积 Canvas 重绘 |

### 15.2 供应链风险

| 风险 | 应对方案 |
|------|----------|
| GL3523 货期长（>4 周）| 备选 CH334R（国产，货期短）先行验证 |
| 长条屏 MIPI 供应商不稳定 | 确认屏幕供应商驱动 IC 资料完整性；备选 RGB 并行接口屏 |
| 3D 打印外壳精度不足 | 原型用 SLA，量产前切换注塑 |

### 15.3 认证周期

| 认证 | 预计周期 | 费用估算 |
|------|----------|----------|
| 3C（中国强制）| 1-2 个月 | ¥0.5-1 万 |
| CE（欧盟）| 2-3 个月 | ¥1-2 万 |
| FCC（美国）| 1-2 个月 | ¥1-1.5 万 |

> USB-HUB 无高压充电电路，认证难度和费用均大幅低于充电站方案。

---

## 十六、商业化方案

> **本项目为开源项目**（硬件设计 + 固件代码全部开源），商业模式基于硬件销售利润 + 社区生态，不依赖软件付费/DLC/会员。

### 16.1 SKU 规划

| 版本 | 配置 | 建议售价 |
|------|------|----------|
| **DIY 套件** | PCB 裸板 + 元器件包（用户自行焊接组装） | ¥199 |
| **成品版** | ESP32-P4 + 长条屏 + USB-A×2 + USB-C×1 + 回卷线 + 外壳 | ¥349 |
| **限定配色版** | 成品版 + 特别配色外壳（樱花粉/深空灰） | ¥379 |

> 开源项目的 SKU 差异化靠硬件配置和外壳工艺，不靠软件锁功能。

### 16.2 盈利模型

```
硬件利润（核心收入）
  成品版售价 ¥349 - BOM ¥313 - 渠道/物流 ¥20 ≈ 毛利 ¥16（5%，小批量）
  量产阶段（BOM ¥250）：毛利 ¥79（23%）
  DIY 套件售价 ¥199 - BOM ¥180 - 物流 ¥10 ≈ 毛利 ¥9（走量+社区引流）

其他收入
  企业定制：公司 Logo + 专属外壳，B 端礼品市场（200+ 台起订，溢价 30-50%）
  众筹溢价：早鸟价即正式售价，众筹阶段利润覆盖模具和认证
  周边配件：额外外壳配色、替换磁吸线头等实体配件
```

> 开源项目的核心逻辑：**硬件赚利润，软件赚口碑**。所有固件功能（能量花园、AI 对话、主题皮肤与后续生态）全部免费开放，靠社区贡献加速迭代，靠品牌认知度卖硬件。

### 16.3 开源策略

| 开源内容 | 协议 | 说明 |
|----------|------|------|
| 固件源码 | MIT / Apache 2.0 | 允许商用和修改 |
| 硬件设计（原理图 + PCB） | CERN-OHL-P | 开源硬件标准协议 |
| 3D 外壳模型 | CC BY-SA 4.0 | 允许修改，需署名 |
| 美术资源（精灵图/动画） | CC BY-NC-SA 4.0 | 非商用共享，保护原创美术 |

**开源带来的优势**：
- 社区贡献新生态模块（养兔/养鸟/种菜），加速内容丰富度
- 开发者自行适配不同屏幕/主控，扩大用户基数
- 透明度建立信任，众筹转化率更高
- 降低维护成本（社区帮忙修 bug、翻译、适配）

**开源的风险与应对**：
- 被山寨：靠品牌、社区、持续迭代速度建立护城河，而非代码壁垒
- 无软件收入：硬件利润必须覆盖所有成本，定价不能过低

### 16.4 众筹策略

| 平台 | 目标人群 | 建议目标金额 |
|------|----------|-------------|
| 摩点（国内）| 开源硬件爱好者 + 数码用户 | ¥20 万 |
| Kickstarter（海外）| Maker + 桌面美化爱好者 | $30,000 |

> 开源项目众筹目标可适当降低——社区信任度高、转化率高，且无需覆盖软件开发的长期成本。

**众筹节奏**
1. **提前 2-3 个月**开始内容预热（B 站制作过程 timelapse + GitHub 开源仓库提前公开 + 小红书桌面美化场景种草）
2. 预热期在 GitHub 积累 Star，目标 1000+ Star 再启动众筹
3. 众筹期 30 天，早鸟价 9 折
4. 众筹结束后 3-4 个月内发货（预留认证 + 量产时间）
5. **发货后 2 个月内 OTA 推送养猫 Beta 版**（社区共建）

**营销漏斗预估**
```
目标：500 人下单（¥175K 达标）
  ↑ 5-8% 转化率（开源项目信任度高）
需要 6,000-10,000 人着陆页访问
  ↑ GitHub Star + 视频曝光
需要约 20-30 万次视频曝光量 + 1000 GitHub Star
```

### 16.5 后续扩展

- **社区贡献生态**：开放生态模块 API，社区开发者可贡献新宠物/植物/鱼类
- **企业定制**：公司 Logo + 专属外壳，B 端礼品市场
- **周边配件**：替换外壳（注塑量产后成本极低）、额外磁吸线头
- **教程/课程**：ESP32-P4 + LVGL 开发教程（免费，引流到硬件销售）
```

### 16.4 后续扩展

- **花园扩展包**：额外生态模块（养兔 / 养鸟 / 种菜），¥29 / 个
- **联名款**：与 IP 合作（三丽鸥 / 宝可梦），授权皮肤
- **企业定制**：公司 Logo + 专属宠物，B 端礼品市场

---

## 十七、UI 资源规划

### 17.1 帧缓冲内存估算

1280×452 长条屏分辨率，内存需求大幅低于原 1080×1920 方案：

| 资源 | 计算 | 大小 |
|------|------|------|
| 单帧缓冲（RGB565）| 1280 × 452 × 2B | **≈ 1.1MB** |
| 双缓冲（零撕裂）| 1.1MB × 2 | **≈ 2.2MB** |
| LVGL 工作缓冲区 | 1/10 屏幕高度 × 宽度 × 2B | **≈ 115KB** |
| 合计 PSRAM 占用 | | **≈ 2.4MB** |

> 结论：1280×452 分辨率对内存压力远小于原方案。**16MB PSRAM 即可满足需求**（2.4MB 帧缓冲 + 业务逻辑 + 音频缓冲），32MB PSRAM 留有充足余量。帧缓冲放 PSRAM，内部 SRAM 留给业务逻辑。

### 17.2 动画资源策略

分层合成方案（适配 1280×452 横屏）：

```
渲染层级（从底到顶）：
  Layer 0：背景层    天空渐变 + 地面，LVGL 渐变绘制，0 Flash 占用
  Layer 1：场景层    花圃/鱼缸/草地，静态 PNG（1280×452），压缩后约 150KB
  Layer 2：角色层    猫/植物/鱼 精灵图，150×150px，20帧序列
  Layer 3：粒子层    LVGL Canvas 实时绘制，0 Flash 占用
  Layer 4：天气层    雨滴/雪花/云朵，小尺寸精灵，约 50KB
  Layer 5：UI 层     状态栏/按钮/文字，LVGL 控件，字体文件
```

### 17.3 Flash 资源估算

| 资源类型 | 内容 | 估算大小 |
|----------|------|----------|
| 猫咪精灵图 | 4 品种 × 6 状态 × 20帧 × 150×150 RGB565 | ≈ 2.1MB |
| 植物精灵图 | 5 种类 × 6 阶段 × 10帧 × 120×150 | ≈ 1.1MB |
| 鱼类精灵图 | 4 种类 × 8帧 × 80×50 | ≈ 0.2MB |
| 背景场景 | 4 季节 × 3 天气 × 静态 PNG（1280×452） | ≈ 0.8MB |
| 天气动效 | 雨/雪/云/晴 小精灵 | ≈ 0.2MB |
| 中文字体 | 思源黑体子集（常用 3500 字）| ≈ 1.5MB |
| 图标资源 | UI 图标 SVG 转位图 | ≈ 0.2MB |
| 音效文件 | 喵/咕噜/浇水/提示音（MP3）| ≈ 0.5MB |
| 固件本体 | ESP-IDF + LVGL + 业务逻辑 | ≈ 2.5MB |
| **合计** | | **≈ 9.1MB** |

> 结论：16MB Flash 完全够用，无需外挂 SPI Flash。相比原 1080×1920 方案资源量减少约 25%，精灵图尺寸缩小适配 452px 屏高。

### 17.4 图片压缩方案

| 格式 | 压缩率 | ESP32-P4 解码速度 | 推荐场景 |
|------|--------|-------------------|----------|
| RGB565 原始 | 1× | 极快（直接 DMA）| 高频动画帧 |
| PNG | 3-5× | 中等（软件解码）| 静态背景 |
| LVGL .bin | 2-3× | 快（LVGL 内置）| 精灵图 |
| JPEG | 8-15× | 中等 | 背景图片 |

推荐方案：动画精灵用 **LVGL .bin 格式**（工具链直接转换），背景用 **JPEG**，UI 图标用 **PNG**。

### 17.5 字体方案

中文显示需要子集化字体，避免全量字库（全量思源黑体 > 20MB）：

```python
# 使用 lv_font_conv 生成子集字体
lv_font_conv --font SourceHanSans.ttf \
  --size 24 \
  --bpp 4 \
  --range 0x4E00-0x9FFF \  # 常用汉字
  --range 0x0020-0x007F \  # ASCII
  -o lv_font_chinese_24.c
```

字号规划：
- 状态栏：18px
- 正文/属性：22px
- 倒计时大字：72px（仅 ASCII 数字，极小）
- AI 对话气泡：24px

---

## 十八、总结

AI 桌面能量花园不是一个 USB-HUB 上加了一块屏，而是一座"住"在扩展坞里的数字花园——你的设备越活跃，花园越有生机。

以 ESP32-P4 为主控、1280×452 长条屏为载体，稳定的 MIPI-DSI + LVGL 渲染保障基础动效，PPA 作为后续验证项，USB 3.0 HUB 功能提供实用价值，游戏化生态成长系统提供情感连接，三者合一构成差异化产品。

### 关键风险与应对

| 优先级 | 风险项 | 应对 |
|--------|--------|------|
| P0 | MIPI-DSI 长条屏驱动 | 立即用 EV Board + 目标屏实测，Phase 1 后 Go/No-Go |
| P0 | MVP 内容深度不足 | 主动交互（浇水/任务/番茄钟）+ 截图分享 + OTA 承诺 |
| P1 | 磁吸回卷线可靠性 | 仅 1 条 Type-C，降低机械复杂度 |
| P1 | V2 跳票 | 拆分为 V2a（猫 Beta）+ V2b（鱼/TTS/App） |
| P2 | C6 单点故障 | 心跳检测 + 自动软复位 |
| P2 | NVS Flash 寿命 | 事件触发 + 15 分钟兜底写入 |

### 下一步行动

1. **立即**：ESP32-P4 官方 EV Board + 1280×452 长条屏点亮 LVGL Demo，验证稳定显示、触摸输入和页面滑动。**这是 P0 验证项，任何加速方案必须先不破坏显示稳定性。**
2. **第 1 周**：确认屏幕供应商，获取驱动 IC 完整资料（Init Sequence）。
3. **第 2 周**：ESP32-C6 UART DMA 通信跑通，验证 AT 指令调用天气 API + 心跳检测。
4. **第 3 周**：LVGL 种花模式横屏 UI 原型，验证帧率 + 中文字体渲染 + 按钮交互。
5. **同步启动**：众筹预热内容制作（制作过程 timelapse、桌面美化场景拍摄）。

---

*文档版本：v10.0 | 更新日期：2026-05-18 | 变更：同步当前 UI 与需求演进——交互改为触摸横向轮播；完善 Focus 像素番茄钟、番茄收获动画、Garden 仓库/种植/肥料池、AI 表情园丁、Device Center 设置中心；记录 ESP32-P4 显示链路以 48MHz DCLK、RGB888、PPA 关闭为稳定基线。*
